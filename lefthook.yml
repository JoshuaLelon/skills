# Deliberately empty — a guard, not a config.
#
# The skills repo has no hooks. This file exists because the spanning
# template's `npm install` (lefthook is one of its devDependencies) triggers
# lefthook's postinstall, which walks up to the nearest .git — this repo —
# installs a marker hook, and would regenerate an example-stub config here if
# this file were absent. An explicit empty config beats a recurring mystery
# stub. The REAL lefthook configs are:
#   production-port/assets/configs/lefthook.yml                      (skill asset)
#   production-port/assets/cloudflare-neon-prod-spanning-template/…  (template's own)
